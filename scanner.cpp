#include "scanner.h"


shared_ptr<Token> Scanner::nextToken()
{
    while(nextChar())
    {
        switch(ch)
        {
            case ' ' :
            brea
        }
    }
    return shared_ptr<Token>();
}
